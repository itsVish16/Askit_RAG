# Secrets
.env
backend/.env

# Python / packaging
.venv
.python-version
__pycache__/
*.pyc
.ruff_cache/
.pytest_cache/

# Lockfile IS tracked (uv.lock should be committed).

# Model caches (several GB; re-downloaded on demand)
.cache/
huggingface_cache/

# Local data — RAGBench parquets are large; fetch from HF instead.
# (sqlite dbs + uploads live here too, runtime-generated.)
backend/data/

# SQLite databases (chat history, auth, ingest status)
*.sqlite
*.sqlite-journal

# Frontend
frontend/node_modules/
frontend/.next/
frontend/.env
frontend/.env.local
frontend/next-env.d.ts
