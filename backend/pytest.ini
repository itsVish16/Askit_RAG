[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = .
addopts = -ra --strict-markers
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning:langchain_community
    ignore::UserWarning
